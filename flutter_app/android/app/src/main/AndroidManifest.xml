<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.flutter_app">
    <!-- DİKKAT: Yukarıdaki package ismi senin projene göre farklı olabilir, değiştirme -->

    <!-- ========================================== -->
    <!-- İZİNLER BURAYA (Application'ın DIŞINA) -->
    <!-- ========================================== -->

    <!-- Backend'e bağlanmak için şart -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Profil fotoğrafı çekmek için -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- Galeriden resim seçmek için (Eski Android sürümleri için gerekebilir) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:label="ToAiDo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"> <!-- HTTP izni için bu şart -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:usesCleartextTraffic="true"
            android:windowSoftInputMode="adjustResize">


            <!-- BURADAKİ YANLIŞ İZİN KODU SİLİNDİ -->

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>